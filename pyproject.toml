[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mine-runtime"
version = "0.1.0"
description = "Mine runtime for crawling, enrichment, schema handling, and Data Mining WorkNet submissions"
requires-python = ">=3.11"
